import NotFound from "@/components/NotFound";

/**
 * 404 route. The page itself is a small demo of the product: a hero-style
 * layout where the site's own icons animate, rather than a generic card.
 * All UI lives in the client component so it can drive motion.
 */
export default function NotFoundPage() {
	return <NotFound />;
}
